created a simple backend server (local) which does a complicated function (calculate sum) and return the output.
other things like headers adding counter is also implemented 
next commits will involves other Request methods and middlewares.
